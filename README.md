Instale todas as dependências através do comando:
  "pip install -r requirements.txt"

Atenção! No arquivo "service.py" adicione a sua chave do groq.

Para executar basta executar o seguinte comando:
  "python3 app.py"
